<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Administrador Node
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                h2 Inicar Sesión
                .campo
                    label(for="email") E-mail:
                    input(type="text" name="email" placeholder="lourdes@mail.com" )
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" placeholder="1234abcd" )
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Sesión")
                .campo.acciones
                    a(href="/crear-cuenta") ¿No tienes cuenta? Crea una gratis
                    a(href="/reestablecer") ¿Olvidaste tu Contraseña?
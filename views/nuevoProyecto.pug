extends layout

include mixins/crearProyecto

block contenido
    .contenido-principal
        h1 #{nombrePagina}

        //- pre= stringObject(proyecto)

        if errores
            each error in errores 
                .alerta= error.texto
        +crearProyecto(proyecto)
        